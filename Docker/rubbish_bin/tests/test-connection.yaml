apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "PlogChart.fullname" . }}-test-connection"
  labels:
    {{- include "PlogChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "PlogChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
